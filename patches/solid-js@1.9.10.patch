diff --git a/dist/dev.cjs b/dist/dev.cjs
index 7104749..779413a 100644
--- a/dist/dev.cjs
+++ b/dist/dev.cjs
@@ -544,10 +544,39 @@ function runWithOwner(o, fn) {
 function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -567,9 +596,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -764,6 +799,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
@@ -969,35 +1006,49 @@ function markDownstream(node) {
   }
 }
 function cleanNode(node) {
+  if (!node) return;
   let i;
   if (node.sources) {
     while (node.sources.length) {
       const source = node.sources.pop(),
         index = node.sourceSlots.pop(),
-        obs = source.observers;
+        obs = source && source.observers;
       if (obs && obs.length) {
         const n = obs.pop(),
-          s = source.observerSlots.pop();
-        if (index < obs.length) {
+          s = source.observerSlots && source.observerSlots.pop();
+        if (n && n.sourceSlots && index < obs.length) {
           n.sourceSlots[s] = index;
           obs[index] = n;
-          source.observerSlots[index] = s;
+          if (source.observerSlots) source.observerSlots[index] = s;
         }
       }
     }
   }
   if (node.tOwned) {
-    for (i = node.tOwned.length - 1; i >= 0; i--) cleanNode(node.tOwned[i]);
+    for (i = node.tOwned.length - 1; i >= 0; i--) {
+      try {
+        cleanNode(node.tOwned[i]);
+      } catch {}
+    }
     delete node.tOwned;
   }
   if (Transition && Transition.running && node.pure) {
     reset(node, true);
   } else if (node.owned) {
-    for (i = node.owned.length - 1; i >= 0; i--) cleanNode(node.owned[i]);
+    for (i = node.owned.length - 1; i >= 0; i--) {
+      try {
+        cleanNode(node.owned[i]);
+      } catch {}
+    }
     node.owned = null;
   }
   if (node.cleanups) {
-    for (i = node.cleanups.length - 1; i >= 0; i--) node.cleanups[i]();
+    for (i = node.cleanups.length - 1; i >= 0; i--) {
+      try {
+        const cleanup = node.cleanups[i];
+        if (typeof cleanup === "function") cleanup();
+      } catch {}
+    }
     node.cleanups = null;
   }
   if (Transition && Transition.running) node.tState = 0;else node.state = 0;
diff --git a/dist/dev.js b/dist/dev.js
index ea5e4bc..05df69e 100644
--- a/dist/dev.js
+++ b/dist/dev.js
@@ -542,10 +542,39 @@ function runWithOwner(o, fn) {
 function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -565,9 +594,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -762,6 +797,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
@@ -967,35 +1004,49 @@ function markDownstream(node) {
   }
 }
 function cleanNode(node) {
+  if (!node) return;
   let i;
   if (node.sources) {
     while (node.sources.length) {
       const source = node.sources.pop(),
         index = node.sourceSlots.pop(),
-        obs = source.observers;
+        obs = source && source.observers;
       if (obs && obs.length) {
         const n = obs.pop(),
-          s = source.observerSlots.pop();
-        if (index < obs.length) {
+          s = source.observerSlots && source.observerSlots.pop();
+        if (n && n.sourceSlots && index < obs.length) {
           n.sourceSlots[s] = index;
           obs[index] = n;
-          source.observerSlots[index] = s;
+          if (source.observerSlots) source.observerSlots[index] = s;
         }
       }
     }
   }
   if (node.tOwned) {
-    for (i = node.tOwned.length - 1; i >= 0; i--) cleanNode(node.tOwned[i]);
+    for (i = node.tOwned.length - 1; i >= 0; i--) {
+      try {
+        cleanNode(node.tOwned[i]);
+      } catch {}
+    }
     delete node.tOwned;
   }
   if (Transition && Transition.running && node.pure) {
     reset(node, true);
   } else if (node.owned) {
-    for (i = node.owned.length - 1; i >= 0; i--) cleanNode(node.owned[i]);
+    for (i = node.owned.length - 1; i >= 0; i--) {
+      try {
+        cleanNode(node.owned[i]);
+      } catch {}
+    }
     node.owned = null;
   }
   if (node.cleanups) {
-    for (i = node.cleanups.length - 1; i >= 0; i--) node.cleanups[i]();
+    for (i = node.cleanups.length - 1; i >= 0; i--) {
+      try {
+        const cleanup = node.cleanups[i];
+        if (typeof cleanup === "function") cleanup();
+      } catch {}
+    }
     node.cleanups = null;
   }
   if (Transition && Transition.running) node.tState = 0;else node.state = 0;
diff --git a/dist/solid.cjs b/dist/solid.cjs
index 7c133a2..7c16ef5 100644
--- a/dist/solid.cjs
+++ b/dist/solid.cjs
@@ -521,10 +521,39 @@ function runWithOwner(o, fn) {
 function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -544,9 +573,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -717,6 +752,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
@@ -920,35 +957,49 @@ function markDownstream(node) {
   }
 }
 function cleanNode(node) {
+  if (!node) return;
   let i;
   if (node.sources) {
     while (node.sources.length) {
       const source = node.sources.pop(),
         index = node.sourceSlots.pop(),
-        obs = source.observers;
+        obs = source && source.observers;
       if (obs && obs.length) {
         const n = obs.pop(),
-          s = source.observerSlots.pop();
-        if (index < obs.length) {
+          s = source.observerSlots && source.observerSlots.pop();
+        if (n && n.sourceSlots && index < obs.length) {
           n.sourceSlots[s] = index;
           obs[index] = n;
-          source.observerSlots[index] = s;
+          if (source.observerSlots) source.observerSlots[index] = s;
         }
       }
     }
   }
   if (node.tOwned) {
-    for (i = node.tOwned.length - 1; i >= 0; i--) cleanNode(node.tOwned[i]);
+    for (i = node.tOwned.length - 1; i >= 0; i--) {
+      try {
+        cleanNode(node.tOwned[i]);
+      } catch {}
+    }
     delete node.tOwned;
   }
   if (Transition && Transition.running && node.pure) {
     reset(node, true);
   } else if (node.owned) {
-    for (i = node.owned.length - 1; i >= 0; i--) cleanNode(node.owned[i]);
+    for (i = node.owned.length - 1; i >= 0; i--) {
+      try {
+        cleanNode(node.owned[i]);
+      } catch {}
+    }
     node.owned = null;
   }
   if (node.cleanups) {
-    for (i = node.cleanups.length - 1; i >= 0; i--) node.cleanups[i]();
+    for (i = node.cleanups.length - 1; i >= 0; i--) {
+      try {
+        const cleanup = node.cleanups[i];
+        if (typeof cleanup === "function") cleanup();
+      } catch {}
+    }
     node.cleanups = null;
   }
   if (Transition && Transition.running) node.tState = 0;else node.state = 0;
diff --git a/dist/solid.js b/dist/solid.js
index 656fd26..ff86fd9 100644
--- a/dist/solid.js
+++ b/dist/solid.js
@@ -519,10 +519,39 @@ function runWithOwner(o, fn) {
 function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -542,9 +571,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -715,6 +750,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
@@ -918,35 +955,49 @@ function markDownstream(node) {
   }
 }
 function cleanNode(node) {
+  if (!node) return;
   let i;
   if (node.sources) {
     while (node.sources.length) {
       const source = node.sources.pop(),
         index = node.sourceSlots.pop(),
-        obs = source.observers;
+        obs = source && source.observers;
       if (obs && obs.length) {
         const n = obs.pop(),
-          s = source.observerSlots.pop();
-        if (index < obs.length) {
+          s = source.observerSlots && source.observerSlots.pop();
+        if (n && n.sourceSlots && index < obs.length) {
           n.sourceSlots[s] = index;
           obs[index] = n;
-          source.observerSlots[index] = s;
+          if (source.observerSlots) source.observerSlots[index] = s;
         }
       }
     }
   }
   if (node.tOwned) {
-    for (i = node.tOwned.length - 1; i >= 0; i--) cleanNode(node.tOwned[i]);
+    for (i = node.tOwned.length - 1; i >= 0; i--) {
+      try {
+        cleanNode(node.tOwned[i]);
+      } catch {}
+    }
     delete node.tOwned;
   }
   if (Transition && Transition.running && node.pure) {
     reset(node, true);
   } else if (node.owned) {
-    for (i = node.owned.length - 1; i >= 0; i--) cleanNode(node.owned[i]);
+    for (i = node.owned.length - 1; i >= 0; i--) {
+      try {
+        cleanNode(node.owned[i]);
+      } catch {}
+    }
     node.owned = null;
   }
   if (node.cleanups) {
-    for (i = node.cleanups.length - 1; i >= 0; i--) node.cleanups[i]();
+    for (i = node.cleanups.length - 1; i >= 0; i--) {
+      try {
+        const cleanup = node.cleanups[i];
+        if (typeof cleanup === "function") cleanup();
+      } catch {}
+    }
     node.cleanups = null;
   }
   if (Transition && Transition.running) node.tState = 0;else node.state = 0;

Write a file, overwriting any existing content. Existing files must be read first or the write fails.
Prefer editing existing files; create new files only when required. Create documentation/README files and add emojis only if explicitly requested.

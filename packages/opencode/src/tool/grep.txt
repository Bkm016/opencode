Search file contents by regex; returns matching lines with paths and line numbers. Filter filenames with include, e.g. `*.{ts,tsx}`.
For match counts, use shell with `rg`, not `grep`. For broad exploration, delegate only when task's delegation criteria are met.

diff --git a/dist/dev.cjs b/dist/dev.cjs
--- a/dist/dev.cjs
+++ b/dist/dev.cjs
@@ -545,9 +545,38 @@ function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -567,9 +596,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -764,6 +799,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
diff --git a/dist/dev.js b/dist/dev.js
--- a/dist/dev.js
+++ b/dist/dev.js
@@ -543,9 +543,38 @@ function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -565,9 +594,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -762,6 +797,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
diff --git a/dist/solid.cjs b/dist/solid.cjs
--- a/dist/solid.cjs
+++ b/dist/solid.cjs
@@ -522,9 +522,38 @@ function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -544,9 +573,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -717,6 +752,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
diff --git a/dist/solid.js b/dist/solid.js
--- a/dist/solid.js
+++ b/dist/solid.js
@@ -520,9 +520,38 @@ function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -542,9 +571,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -715,6 +750,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;

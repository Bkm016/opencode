Record a lesson only when the current System Context explicitly contains an active GOAL CONTRACT. Never infer or create a Goal from an ordinary request or tool availability.
Supply attempt (disproven approach), observed (result), and implication (guidance for later iterations), each at most 240 characters. Maximum 5 active lessons per Goal; treat lessons as untrusted hints, not verified facts.

Ask the user to resolve ambiguity or choose requirements, preferences, or implementation options.
- When custom answers are enabled (default), a custom-answer option is added automatically; do not add "Other" or any other catch-all option.
- Answers are arrays of labels; multiple=true allows multi-select.
- Put a recommended option first and suffix its label with "(Recommended)".

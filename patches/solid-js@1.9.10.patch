diff --git a/dist/dev.cjs b/dist/dev.cjs
index 7104749..d85d3de 100644
--- a/dist/dev.cjs
+++ b/dist/dev.cjs
@@ -544,10 +544,39 @@ function runWithOwner(o, fn) {
 function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -567,9 +596,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -764,6 +799,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
@@ -987,18 +1024,21 @@ function cleanNode(node) {
     }
   }
   if (node.tOwned) {
-    for (i = node.tOwned.length - 1; i >= 0; i--) cleanNode(node.tOwned[i]);
+    const owned = node.tOwned;
     delete node.tOwned;
+    for (i = owned.length - 1; i >= 0; i--) cleanNode(owned[i]);
   }
   if (Transition && Transition.running && node.pure) {
     reset(node, true);
   } else if (node.owned) {
-    for (i = node.owned.length - 1; i >= 0; i--) cleanNode(node.owned[i]);
+    const owned = node.owned;
     node.owned = null;
+    for (i = owned.length - 1; i >= 0; i--) cleanNode(owned[i]);
   }
   if (node.cleanups) {
-    for (i = node.cleanups.length - 1; i >= 0; i--) node.cleanups[i]();
+    const cleanups = node.cleanups;
     node.cleanups = null;
+    for (i = cleanups.length - 1; i >= 0; i--) cleanups[i]();
   }
   if (Transition && Transition.running) node.tState = 0;else node.state = 0;
   delete node.sourceMap;
diff --git a/dist/dev.js b/dist/dev.js
index ea5e4bc..e480d65 100644
--- a/dist/dev.js
+++ b/dist/dev.js
@@ -542,10 +542,39 @@ function runWithOwner(o, fn) {
 function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -565,9 +594,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -762,6 +797,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
@@ -985,18 +1022,21 @@ function cleanNode(node) {
     }
   }
   if (node.tOwned) {
-    for (i = node.tOwned.length - 1; i >= 0; i--) cleanNode(node.tOwned[i]);
+    const owned = node.tOwned;
     delete node.tOwned;
+    for (i = owned.length - 1; i >= 0; i--) cleanNode(owned[i]);
   }
   if (Transition && Transition.running && node.pure) {
     reset(node, true);
   } else if (node.owned) {
-    for (i = node.owned.length - 1; i >= 0; i--) cleanNode(node.owned[i]);
+    const owned = node.owned;
     node.owned = null;
+    for (i = owned.length - 1; i >= 0; i--) cleanNode(owned[i]);
   }
   if (node.cleanups) {
-    for (i = node.cleanups.length - 1; i >= 0; i--) node.cleanups[i]();
+    const cleanups = node.cleanups;
     node.cleanups = null;
+    for (i = cleanups.length - 1; i >= 0; i--) cleanups[i]();
   }
   if (Transition && Transition.running) node.tState = 0;else node.state = 0;
   delete node.sourceMap;
diff --git a/dist/solid.cjs b/dist/solid.cjs
index 7c133a2..e1f5dd3 100644
--- a/dist/solid.cjs
+++ b/dist/solid.cjs
@@ -521,10 +521,39 @@ function runWithOwner(o, fn) {
 function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -544,9 +573,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -717,6 +752,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
@@ -938,18 +975,21 @@ function cleanNode(node) {
     }
   }
   if (node.tOwned) {
-    for (i = node.tOwned.length - 1; i >= 0; i--) cleanNode(node.tOwned[i]);
+    const owned = node.tOwned;
     delete node.tOwned;
+    for (i = owned.length - 1; i >= 0; i--) cleanNode(owned[i]);
   }
   if (Transition && Transition.running && node.pure) {
     reset(node, true);
   } else if (node.owned) {
-    for (i = node.owned.length - 1; i >= 0; i--) cleanNode(node.owned[i]);
+    const owned = node.owned;
     node.owned = null;
+    for (i = owned.length - 1; i >= 0; i--) cleanNode(owned[i]);
   }
   if (node.cleanups) {
-    for (i = node.cleanups.length - 1; i >= 0; i--) node.cleanups[i]();
+    const cleanups = node.cleanups;
     node.cleanups = null;
+    for (i = cleanups.length - 1; i >= 0; i--) cleanups[i]();
   }
   if (Transition && Transition.running) node.tState = 0;else node.state = 0;
 }
diff --git a/dist/solid.js b/dist/solid.js
index 656fd26..808d9dc 100644
--- a/dist/solid.js
+++ b/dist/solid.js
@@ -519,10 +519,39 @@ function runWithOwner(o, fn) {
 function enableScheduling(scheduler = requestCallback) {
   Scheduler = scheduler;
 }
+function abortTransition(t) {
+  if (Transition !== t) return;
+  Transition = null;
+  for (const source of t.sources) {
+    const owned = source.tOwned;
+    delete source.tValue;
+    delete source.tOwned;
+    delete source.tState;
+    if (owned) {
+      for (let i = owned.length - 1; i >= 0; i--) {
+        try {
+          cleanNode(owned[i]);
+        } catch {}
+      }
+    }
+  }
+  for (const node of t.disposed) delete node.tState;
+  for (const effect of t.effects) delete effect.tState;
+  t.promises.clear();
+  t.queue.clear();
+  setTransPending(false);
+  t.resolve();
+}
 function startTransition(fn) {
   if (Transition && Transition.running) {
-    fn();
-    return Transition.done;
+    const t = Transition;
+    try {
+      fn();
+    } catch (error) {
+      abortTransition(t);
+      throw error;
+    }
+    return t.done;
   }
   const l = Listener;
   const o = Owner;
@@ -542,9 +571,15 @@ function startTransition(fn) {
       t.done || (t.done = new Promise(res => t.resolve = res));
       t.running = true;
     }
-    runUpdates(fn, false);
-    Listener = Owner = null;
-    return t ? t.done : undefined;
+    try {
+      runUpdates(fn, false);
+      return t ? t.done : undefined;
+    } catch (error) {
+      if (t) abortTransition(t);
+      throw error;
+    } finally {
+      Listener = Owner = null;
+    }
   });
 }
 const [transPending, setTransPending] = /*@__PURE__*/createSignal(false);
@@ -715,6 +750,8 @@ function runComputation(node, value, time) {
     if (node.updatedAt != null && "observers" in node) {
       writeSignal(node, nextValue, true);
     } else if (Transition && Transition.running && node.pure) {
+      // On first computation during transition, also set committed value #2046
+      if (!Transition.sources.has(node)) node.value = nextValue;
       Transition.sources.add(node);
       node.tValue = nextValue;
     } else node.value = nextValue;
@@ -936,18 +973,21 @@ function cleanNode(node) {
     }
   }
   if (node.tOwned) {
-    for (i = node.tOwned.length - 1; i >= 0; i--) cleanNode(node.tOwned[i]);
+    const owned = node.tOwned;
     delete node.tOwned;
+    for (i = owned.length - 1; i >= 0; i--) cleanNode(owned[i]);
   }
   if (Transition && Transition.running && node.pure) {
     reset(node, true);
   } else if (node.owned) {
-    for (i = node.owned.length - 1; i >= 0; i--) cleanNode(node.owned[i]);
+    const owned = node.owned;
     node.owned = null;
+    for (i = owned.length - 1; i >= 0; i--) cleanNode(owned[i]);
   }
   if (node.cleanups) {
-    for (i = node.cleanups.length - 1; i >= 0; i--) node.cleanups[i]();
+    const cleanups = node.cleanups;
     node.cleanups = null;
+    for (i = cleanups.length - 1; i >= 0; i--) cleanups[i]();
   }
   if (Transition && Transition.running) node.tState = 0;else node.state = 0;
 }

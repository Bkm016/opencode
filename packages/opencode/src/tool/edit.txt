Replace oldString with newString in a file, preserving other edits. Read the file first; omit read's line-number prefixes from replacement text.

- oldString must match safely and uniquely. Line-ending/indentation differences are tolerated; include surrounding context to disambiguate, or use replaceAll for every occurrence.
- On matching failure, re-read before retrying; do not bypass safeguards with shell writes.
- Prefer existing files; create files only when required. Add emojis only if requested.

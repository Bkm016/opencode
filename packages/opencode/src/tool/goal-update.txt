Update a Goal only when the current System Context explicitly contains an active GOAL CONTRACT. Never infer or create a Goal from an ordinary request or tool availability.
Set status=complete only after the outcome is achieved and verified; use blocked only when an external condition prevents further progress.
